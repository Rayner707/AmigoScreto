# Copia este archivo a .env.local y reemplaza los valores por secretos reales.

# Se usa para generar tokens y el derangement de forma determinística.
TOKEN_SECRET=pon-un-uuid-o-64-caracteres-aleatorios

# Clave para consumir /api/admin/export y obtener los enlaces.
ADMIN_SECRET=admin-super-secreto

# Dirección pública de la app (ajusta en producción, ej. https://tudominio.vercel.app).
PUBLIC_BASE_URL=http://localhost:3000

# Credenciales de Vercel KV (Upstash) para persistir asignaciones y claims.
# En Vercel se configuran al crear el add-on de KV.
# KV_REST_API_URL=https://us1-perseus-0.something.upstash.io
# KV_REST_API_TOKEN=xxxxxxx
